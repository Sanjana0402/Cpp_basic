#include<iostream>

/*
    lvalue : Tokens which have a name given by the programmer(specific name)band the have memory allocated to them

    rvalues : Tokens which don't have memory allocated to them and must be attached (binded) to a memory location before being discarded.
    
    
    // we have to save it somewhere as it is a temporary value as the compiler will discard it after use


    Anything that a function returns is a r value so it has to be saved so that we donot loose it
*/

int main(){
    int n1 = 10 ; //tokens??  tot = 5

    int arr[2] = {10,20};

    //arr is a lvalue??
}